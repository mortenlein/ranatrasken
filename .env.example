# SQLite database used by Prisma (NextAuth users/sessions and stamps)
DATABASE_URL="file:./dev.db"

# NextAuth secret — generate one with: npx auth secret
AUTH_SECRET=""

# Free key from https://www.maptiler.com/ (used for base map + 3D terrain tiles)
NEXT_PUBLIC_MAPTILER_KEY=""
